# TIL-seongchan-Backend
# DBMS란?
- 데이터 베이스라는 데이터의 집합을 만들고, 저장 및 관리할 수 있는 기능들을 제공하는 응용 프로그램
- 주로 기업에서 전반적인 전산 및 데이터 관리를 위해서 대규모로 굴리는 경우가 많음
- 데이터를 “안전하게 저장하고 빠르게 찾고 동시에 여러 사람이 써도 문제 없게 관리해주는 시스템”
### 비유
- 데이터 = 물건
- 데이터 베이스 = 창고
- DBMS = 창고 관리자
## DBMS의 기능
### 정의(Definition) 기능:
모든 응용 프로그램들이 요구하는 데이터 구조를 지원하기 위해 데이터베이스에 저장될 데이터의 형(Type)과 구조에 대한 정의, 이용 방식, 제약 조건 등을 명시하는 기능입니다.
### 조작(Manipulation) 기능:
데이터 검색, 갱신, 삽입, 삭제 등을 체계적으로 처리하기 위해 사용자와 데이터베이스 사이의 인터페이스 수단을 제공하는 기능입니다.
### 제어(Control) 기능자세히 알아보기컴퓨터 서버소프트웨어운영체제os운영 체제:
데이터베이스를 접근하는 갱신, 삽입, 삭제 작업이 정확하게 수행되어 데이터의 무결성이 유지되도록 제어해야 합니다. 정당한 사용자가 허가된 데이터에 접근할 수 있도록 보안을 유지하고 권한을 검사할 수 있어야 합니다.
## [DBMS의 장/단점]
### (장점)
- 데이터의 논리적, 물리적 독립성이 보장됩니다.
- 데이터의 중복을 피할 수 있어서 기억공간이 절약됩니다.
- 저장된 자료를 공동으로 사용할 수 있습니다.
- 데이터의 일관성, 무결성을 유지할 수 있습니다- 보안이 유지됩니다.
- 데이터를 표준화, 통합 관리가 가능합니다.
- 최신의 데이터를 유지할 수 있고, 데이터의 실시간 처리가 가능합니다.
### (단점)
- 데이터베이스의 전문가의 부족- 전산하 비용이 증가합니다.
- 대용량 디스크로의 집중적인 접근(Access)로 과부하가 발생할 수 있습니다.
- 파일의 예비와 백업이 어려운 점이 있습니다.
## DBMS의 내부 구조
1. Query Processor

-> SQL 해석 담당
- 문법 검사
- 실행 계획 생성
2. Storage Manager

-> 실제 데이터 관리
- 파일 관리
- 인덱스 관리
3. Transaction Manager

-> 트래잭션 관리
- commit/rollback
4. Buffer Manager

-> 메모리 관리
## DBMS의 종류
##### RDBMS (관계형)-나눠서 연결
데이터를 테이블로 나눔

서로 관계로 연결

<img width="829" height="221" alt="image" src="https://github.com/user-attachments/assets/96d9a1b2-bbf1-4683-be2d-381d614a9096" />
여기서 user_id -> user.id에 연결됨 
이걸 관계라고 함

<img width="841" height="178" alt="image" src="https://github.com/user-attachments/assets/d559f021-2b48-4eca-b997-341511b622dc" />

위 두 사진 처럼 나눠서 연결함





#####대표:
MySQL, PostgreSQL

테이블 구조, 관계 있음, SQL 사용

##### NoSQL(비관계형)-묶어서 연결
-데이터를 자유롭게 저장
-관계를 강제 하지 않음

#### 대표:
MongoDB

 JSON 구조, 유연함, 속도 빠름

비관계형 DBMS는 데이터를 한번에 저장한다
#### 특징
1. 스키마가 없다(그래서 구조가 자유로움)

<img width="751" height="478" alt="image" src="https://github.com/user-attachments/assets/96cfb7a8-020b-49ac-a1e2-75d6f680b887" />

2. 데이터 중복 허용
-> 관계 대신 데이터를 안에 넣음

3. 빠른 속도
-> JOIN 없음 -> 빠름

4. 수평 확장
-> 서버 여러 개로 나눠서 처리

#### 관게형DBMS와 비관계형DBMS 한방비교!!!

<img width="833" height="441" alt="image" src="https://github.com/user-attachments/assets/23d9971e-f415-486d-ad94-396a96bdfb32" />


##### 여기서 스키마는 뭘까요?
-데이터베이스의 구조(설계도)
##### 쉽게 말해
데이터를 어떻게 저장할지 미리 저장해놓은 규칙이다.
##### 비유
- 설계도 = 스키마
- 건물 = 데이터

만약 설계도 없이 건물을 지으면 구조가 엉망이고 무너지기 쉽다. 그래서 스키마를 쓰는것이다.
##### 스키마 사용

<img width="875" height="705" alt="image" src="https://github.com/user-attachments/assets/8d6b3706-8de3-4b2b-9f28-8372c5169a85" />


#### JSON
JSON은 데이터를 표현하는 “형식(포맷)”이다

 JSON은 프로그램이 아니라

❌ 기능이 있는 것 아님

❌ 데이터를 저장하는 DB도 아님


 그냥:

“데이터를 어떻게 생긴 형태로 쓸지 정한 규칙”
### 비유 
-JSON = 박스 안에 정리된 물건 형태
-HTTP = 배송 시스템
-DBMS = 창
#### 흐름
1. 창고(DBMS)에서 데이터 꺼냄
2. JSON으로 포장
3. HTTP로 배송

한마디로 JSON은 데이터를 주고 받기 위해 사용하는 표준 형식이다.









